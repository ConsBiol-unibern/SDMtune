# The function is verbose

    Code
      checkMaxentInstallation(verbose = TRUE)
    Message
      Java is installed.
      The packege "rJava" is installed.
      The file "maxent.jar" is present in the correct folder.
    Output
      [1] TRUE

