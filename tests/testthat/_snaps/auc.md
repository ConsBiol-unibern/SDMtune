# The function raises warnings and errors

    Code
      auc(m, SDMtune:::t@data)
    Condition
      Error in `.compute_auc()`:
      ! `test` must be an <SWD> object
      x You have supplied a <data.frame> instead.

---

    Code
      auc(m_cv, SDMtune:::t@data)
    Condition
      Error in `.compute_auc()`:
      ! `test` must be an <SWD> object
      x You have supplied a <data.frame> instead.

