# The correct fc arguments are created

    Code
      .get_fc_args("lb")
    Condition
      Error in `.get_fc_args()`:
      ! Possible values for feature classes are `lqpht`
      x You have supplied a `lb` instead.

