# The function raises errors

    ! `test` must be an <SWD> object
    x You have supplied a <data.frame> instead.

---

    ! `test` must be an <SWD> object
    x You have supplied a <data.frame> instead.

