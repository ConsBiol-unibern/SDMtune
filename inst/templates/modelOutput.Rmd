---
output:
  prettydoc::html_pretty:
    theme: cayman
    highlight: github
params:
  model: base_model
  type: type
  test: test
---

```{r, echo=F}
library(SDMsel)
```

**`r class(model@model)`** output produced using **SDMsel** version `r packageVersion("SDMsel")` (Vignali S. *et al.*, 2018).

# ROC curve

Next we plot the ROC curve for `r model@presence@species`:

```{r plot ROC, echo=F, fig.align="center"}
plotROC(params$model, test = params$test)
```

# Thresholds

```{r, echo=F}
knitr::kable(thresholds(params$model, type = params$type, test = params$test))
```

# Response curves

```{r, fig.hold='hold', out.width='20%'}
vars <- colnames(params$model@presence@data)
for (var in vars) {
  plotResponse(params$model, var, type = params$type, marginal = TRUE, rug = TRUE)
}
```


# Reference

- Sergio Vignali, Arnaud Barras and Veronika Braunisch (2018). SDMsel: Species Distribution Model Selection. R package version 0.0.0.9000
